其实就是：字节流读取文字字节数据后，不直接操作而是先查指定的编码表，获取对应的文字，再对这个文字进行操作，简单说：字节流+编码表。

----------------------------------------

字节流的两个顶层父类：
1，InputStream    2，OutputStream

字符流的两个顶层父类：
1，Reader         2，Writer

就从熟悉的文字开始字符流：
//需求：将一些文字存储到硬盘中的一个文件中

记住：如果要操作文字数据，建议优先考虑字符流。而且要将数据从